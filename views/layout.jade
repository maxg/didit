doctype
html
  head
    block head
      link(rel='stylesheet', href='//netdna.bootstrapcdn.com/twitter-bootstrap/2.2.2/css/bootstrap.no-icons.min.css')
      link(rel='stylesheet', href='/style.css')
  body
    div.navbar.navbar-fixed-top
      div.navbar-inner
        div.container-fluid
          ul.nav
            li: a(href='/') The Classroom Continuous Build Butler - Didit
          p.navbar-text.pull-right
            if locals.authstaff
              span.label staff
            | &nbsp;
            a(href="/u/#{locals.authuser}")= locals.authuser
    div.container-fluid
      div.row-fluid
        block content
      hr
      footer
        p.pull-right: small MIT EECS
        - function a(arr) { return arr[Math.floor(Math.random() * arr.length)]; }
        p.muted
          small #{a(config.footer.who)} in the #{a(config.footer.where)} with the #{a(config.footer.what)}?
